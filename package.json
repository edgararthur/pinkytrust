{
  "name": "pinkytrust-monorepo",
  "private": true,
  "workspaces": [
    "user-app",
    "shared",
    "municipal-app",
    "organiser-app"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && npm run build --workspace=user-app",
    "dev": "npm run dev --workspace=user-app"
  }
}
