# PinkyTrust Platform Environment Configuration Template
# Copy this file to create environment files for each application

# =============================================================================
# SUPABASE CONFIGURATION (Required for all applications)
# =============================================================================
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# =============================================================================
# APPLICATION-SPECIFIC CONFIGURATION
# =============================================================================

# User App (PWA) - user-app/.env.local
NEXT_PUBLIC_APP_URL=https://app.pinkytrust.com
NEXT_PUBLIC_APP_NAME=PinkyTrust
NEXT_PUBLIC_ENABLE_PWA=true
NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true
NEXT_PUBLIC_ENABLE_OFFLINE_MODE=true

# Municipal App (Admin) - municipal-app/.env.local
NEXT_PUBLIC_APP_URL=https://municipal.pinkytrust.com
NEXT_PUBLIC_APP_NAME=PinkyTrust Municipal Office
NEXT_PUBLIC_ENABLE_ADMIN_FEATURES=true

# Organiser App (Vite) - organiser-app/.env.local
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_APP_URL=https://organiser.pinkytrust.com
VITE_APP_NAME=PinkyTrust Organiser

# =============================================================================
# EXTERNAL SERVICES (Optional)
# =============================================================================
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your_mapbox_token
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=your_ga_id

# =============================================================================
# FEATURE FLAGS
# =============================================================================
NEXT_PUBLIC_ENABLE_ANALYTICS=true
NEXT_PUBLIC_ENABLE_MAPS=true
NEXT_PUBLIC_ENABLE_QR_SCANNER=true

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# INSTRUCTIONS FOR SETUP
# =============================================================================
# 1. Copy this file to each application directory as .env.local
# 2. Update the values with your actual credentials
# 3. Never commit .env.local files to version control
# 4. For production, set these as environment variables in your deployment platform

# User App:
# cp env.template user-app/.env.local

# Municipal App:
# cp env.template municipal-app/.env.local

# Organiser App:
# cp env.template organiser-app/.env.local 